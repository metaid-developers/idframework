### 针对test/id-connect-button这个文件下的代码架构，写一份metaapp的开发指南

1. 其中该项目使用了alpine.js + unocss + idframework前端库进行开发，你需要了解整个idframework在做什么，idframework的职责是什么，他是如何工作的

2. 说明一下该项目的MVC架构是如何设计的，针对面向AI和开发者开发需要注意的事项

3. 如何让AI更好地去了解这个框架来开发MetaApp，设计idframework初衷是为了更好地把metaapp的组件进行分离，按需引入，抛弃编译原理直接运行，告诉AI开发助手如何把生成的组件引入到index.html中使用

4. commands和idcomponents的仓库目录分别要处理哪些业务

5. 宗旨是在AI开发项目中内置一些基础的commands和idcomponent和idframework.js，其中idframework.js是每个MetaApp项目必要的文件，用户可以通过与agent对话生成项目所需要的index.html,app.css,app.js，agent要去理解学习怎样编写index.html来引入idframework.js，内置的commands和idcomponent以及根据用户需求通过agent生成的commands和idcomponent如何引入到index.html中使用，同理app.js主要负责注册内置commands和动态生成的commands，agent也要学会怎样编写app.js的注册，而app.css是全局的样式文件，直接引入使用即可

6. 就上述的描述帮我在项目doc中生成一份开发指南，我需要为后续agent开发提供一份详细指南和开发规范